import { useCallback, useEffect, useState, type FormEvent } from "react";
import { Link } from "react-router-dom";

import { getApiErrorMessage } from "../services/api";
import { completeDashboardTask, createDashboardTask, createOtherSale, deleteDashboardTask, deleteOtherSale, getCustomers, getDashboardStats, getDashboardTasks, getOtherSales, getSalesTargetProgress, updateOtherSale, updateSalesTarget } from "../services/crm";
import { useAuth } from "../store/auth";
import type { CalculatedFollowupReminderStatus, Customer, DashboardStats, DashboardTask, FollowUpReminder, OtherSalesAmount, SalesTargetProgress, TaskPriority } from "../types";

const priorityLabels: Record<TaskPriority, string> = { high: "高", medium: "中", low: "低" };
const priorityStyles: Record<TaskPriority, string> = { high: "bg-rose-100 text-rose-700", medium: "bg-amber-100 text-amber-700", low: "bg-slate-100 text-slate-700" };
const money = (value: string) => Number(value).toLocaleString(undefined, { maximumFractionDigits: 2 });

function FollowUpReminderList({ items, emptyText }: { items: FollowUpReminder[]; emptyText: string }) {
  if (!items.length) return <p className="text-sm text-slate-500">{emptyText}</p>;
  return <div className="space-y-2">{items.slice(0, 3).map((item) => {
    const overdue = item.reminder_status === "overdue";
    return <Link key={item.id} to={`/customers/${item.customer_id}`} className={`block rounded-lg p-2.5 text-sm ${overdue ? "bg-rose-50 hover:bg-rose-100" : "bg-amber-50 hover:bg-amber-100"}`}><div className="flex justify-between gap-2"><strong className="truncate">{item.customer_name}</strong><span className={overdue ? "whitespace-nowrap text-rose-700" : "whitespace-nowrap text-amber-700"}>{item.next_followup_date}</span></div><p className="mt-1 truncate text-slate-600">{item.type} · {item.content}</p></Link>;
  })}</div>;
}

export function DashboardPage() {
  const { user } = useAuth();
  const isAdmin = user?.role === "Admin";
  const [stats, setStats] = useState<DashboardStats | null>(null);
  const [tasks, setTasks] = useState<DashboardTask[]>([]);
  const [customers, setCustomers] = useState<Customer[]>([]);
  const [salesProgress, setSalesProgress] = useState<SalesTargetProgress | null>(null);
  const [otherSales, setOtherSales] = useState<OtherSalesAmount[]>([]);
  const [title, setTitle] = useState(""); const [dueDate, setDueDate] = useState(""); const [priority, setPriority] = useState<TaskPriority>("medium"); const [customerId, setCustomerId] = useState("");
  const [busy, setBusy] = useState(false); const [error, setError] = useState("");
  const [goalAmount, setGoalAmount] = useState(""); const [otherDate, setOtherDate] = useState(""); const [otherAmount, setOtherAmount] = useState(""); const [otherCurrency, setOtherCurrency] = useState(""); const [otherNote, setOtherNote] = useState(""); const [editingOtherId, setEditingOtherId] = useState<number | null>(null);

  const load = useCallback(async () => {
    try {
      const dashboard = await getDashboardStats();
      setStats(dashboard);
      if (isAdmin) setTasks(await getDashboardTasks());
      const progress = await getSalesTargetProgress();
      setSalesProgress(progress);
      setGoalAmount(progress.annual_target?.target_amount ?? "");
      setOtherCurrency((current) => current || progress.currency);
      setOtherSales(await getOtherSales(progress.year));
      setError("");
    } catch (requestError: unknown) { setError(getApiErrorMessage(requestError, "无法加载仪表盘数据，请刷新后重试。")); }
  }, [isAdmin]);
  useEffect(() => { void load(); }, [load]);
  useEffect(() => { if (isAdmin) void getCustomers({ limit: 100, offset: 0 }).then((page) => setCustomers(page.items)).catch(() => undefined); }, [isAdmin]);

  async function createTask(event: FormEvent) {
    event.preventDefault(); if (!title.trim() || !dueDate) return;
    setBusy(true); try { await createDashboardTask({ title: title.trim(), due_date: dueDate, priority, customer_id: customerId ? Number(customerId) : undefined }); setTitle(""); setCustomerId(""); await load(); } catch (requestError: unknown) { setError(getApiErrorMessage(requestError, "无法新增待办。")); } finally { setBusy(false); }
  }
  async function completeTask(id: number) { setBusy(true); try { await completeDashboardTask(id); await load(); } catch (requestError: unknown) { setError(getApiErrorMessage(requestError, "无法完成待办。")); } finally { setBusy(false); } }
  async function deleteTask(id: number) { setBusy(true); try { await deleteDashboardTask(id); await load(); } catch (requestError: unknown) { setError(getApiErrorMessage(requestError, "无法删除待办。")); } finally { setBusy(false); } }
  async function saveGoal(event: FormEvent) { event.preventDefault(); if (!salesProgress || !goalAmount) return; setBusy(true); try { await updateSalesTarget(salesProgress.year, goalAmount); await load(); } catch (requestError: unknown) { setError(getApiErrorMessage(requestError, "无法保存年度目标。")); } finally { setBusy(false); } }
  async function saveOtherSale(event: FormEvent) { event.preventDefault(); if (!otherDate || !otherAmount || !otherCurrency) return; setBusy(true); const payload = { sale_date: otherDate, amount: otherAmount, currency: otherCurrency, note: otherNote }; try { if (editingOtherId) await updateOtherSale(editingOtherId, payload); else await createOtherSale(payload); setOtherDate(""); setOtherAmount(""); setOtherNote(""); setEditingOtherId(null); await load(); } catch (requestError: unknown) { setError(getApiErrorMessage(requestError, "无法保存其他销售额。")); } finally { setBusy(false); } }
  function beginEditOtherSale(item: OtherSalesAmount) { setEditingOtherId(item.id); setOtherDate(item.sale_date); setOtherAmount(item.amount); setOtherCurrency(item.currency); setOtherNote(item.note); }
  async function removeOtherSale(id: number) { setBusy(true); try { await deleteOtherSale(id); if (editingOtherId === id) setEditingOtherId(null); await load(); } catch (requestError: unknown) { setError(getApiErrorMessage(requestError, "无法删除其他销售额。")); } finally { setBusy(false); } }

  const cards: Array<[string, number | undefined, string, CalculatedFollowupReminderStatus | undefined]> = [
    ["已逾期", stats?.followup_reminder_overdue_count, "text-rose-700", "overdue"],
    ["今日需要跟进", stats?.followup_reminder_today_count, "text-orange-700", "today"],
    ["未来3天", stats?.followup_reminder_upcoming_count, "text-amber-700", "upcoming"],
    ["客户总数", stats?.customer_count, "text-slate-800", undefined],
  ];

  return <>
    <p className="text-sm text-slate-500">欢迎回来，{user?.name}</p>
    <div className="mt-1 flex flex-wrap items-end justify-between gap-3"><h2 className="text-3xl font-bold">仪表盘</h2>{user?.role !== "Viewer" && <Link to="/customers/new" className="rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white">新增客户</Link>}</div>
    {error && <p className="mt-4 text-sm text-rose-600" role="alert">{error}</p>}
    <section className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([titleText, value, color, reminderFilter]) => {
      const content = <><p className="text-sm text-slate-500">{titleText}</p><p className={`mt-2 text-3xl font-bold ${color}`}>{value ?? "—"}</p></>;
      const className = "rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200";
      return reminderFilter ? <Link key={titleText} to={`/followup-reminders?status=${reminderFilter}`} className={`${className} transition hover:-translate-y-0.5 hover:shadow-md`}>{content}</Link> : <article key={titleText} className={className}>{content}</article>;
    })}</section>
    <section className="mt-5 grid gap-5 xl:grid-cols-2">
      <article className="rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex items-center justify-between"><div><h3 className="font-bold">今日待办</h3><p className="mt-1 text-sm text-slate-500">今天到期和已逾期的任务</p></div>{isAdmin && <span className="text-sm text-slate-500">{tasks.length} 项</span>}</div>{isAdmin ? <><form onSubmit={(event) => void createTask(event)} className="mt-4 grid gap-2 sm:grid-cols-2"><input required value={title} onChange={(event) => setTitle(event.target.value)} placeholder="待办标题" className="rounded border px-3 py-2 text-sm" /><input required type="date" value={dueDate} onChange={(event) => setDueDate(event.target.value)} className="rounded border px-3 py-2 text-sm" /><select value={priority} onChange={(event) => setPriority(event.target.value as TaskPriority)} className="rounded border px-3 py-2 text-sm"><option value="high">高优先级</option><option value="medium">中优先级</option><option value="low">低优先级</option></select><select value={customerId} onChange={(event) => setCustomerId(event.target.value)} className="rounded border px-3 py-2 text-sm"><option value="">不关联客户</option>{customers.map((customer) => <option key={customer.id} value={customer.id}>{customer.company_name}</option>)}</select><button disabled={busy} className="w-fit rounded bg-blue-700 px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">新增待办</button></form><div className="mt-3 divide-y">{tasks.map((task) => <div key={task.id} className="flex items-center justify-between gap-3 py-2.5"><div className="min-w-0"><p className="truncate text-sm font-medium">{task.title}</p><p className="text-xs text-slate-500">截止 {task.due_date}{task.customer_name ? ` · ${task.customer_name}` : ""}</p></div><div className="flex shrink-0 items-center gap-2"><span className={`rounded-full px-2 py-1 text-xs font-semibold ${priorityStyles[task.priority]}`}>{priorityLabels[task.priority]}</span><button type="button" disabled={busy} onClick={() => void completeTask(task.id)} className="text-xs font-semibold text-emerald-700">完成</button><button type="button" disabled={busy} onClick={() => void deleteTask(task.id)} className="text-xs text-rose-600">删除</button></div></div>)}{!tasks.length && <p className="py-5 text-center text-sm text-slate-500">今日没有待办任务。</p>}</div></> : <p className="mt-4 text-sm text-slate-500">待办仅对 Admin 开放。</p>}</article>
      <article className="rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex items-center justify-between"><div><h3 className="font-bold">客户跟进提醒</h3><p className="mt-1 text-sm text-slate-500">优先处理最紧急客户</p></div><Link to="/followup-reminders" className="text-sm text-blue-700">提醒中心</Link></div><h4 className="mt-4 text-sm font-semibold text-rose-700">逾期未跟进</h4><div className="mt-2"><FollowUpReminderList items={stats?.overdue_followups ?? []} emptyText="暂无逾期客户。" /></div><h4 className="mt-4 text-sm font-semibold text-amber-700">今日待跟进</h4><div className="mt-2"><FollowUpReminderList items={stats?.today_followups ?? []} emptyText="今日暂无待跟进客户。" /></div></article>
    </section>
    <section className="mt-5 rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div className="flex flex-wrap items-start justify-between gap-3"><div><h3 className="font-bold">销售目标进度</h3><p className="mt-1 text-sm text-slate-500">{salesProgress?.year ?? "—"} 年 · 目标币种 {salesProgress?.currency ?? "—"}</p></div>{user?.role !== "Viewer" && <form onSubmit={(event) => void saveGoal(event)} className="flex items-center gap-2"><input required type="number" min="0" step="0.01" value={goalAmount} onChange={(event) => setGoalAmount(event.target.value)} placeholder="年度目标" className="w-32 rounded border px-2 py-1.5 text-sm" /><button disabled={busy} className="rounded bg-blue-700 px-3 py-1.5 text-sm font-semibold text-white disabled:opacity-60">保存目标</button></form>}</div><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{salesProgress?.periods.map((period) => { const percent = Math.min(100, Number(period.completion_percent ?? 0)); return <div key={period.key} className="rounded-lg bg-slate-50 p-4"><div className="flex justify-between text-sm"><strong>{period.label}</strong><span>{period.completion_percent === null ? "未设目标" : `${Number(period.completion_percent).toFixed(1)}%`}</span></div><p className="mt-3 text-sm">{salesProgress.currency} {money(period.actual_amount)} <span className="text-slate-500">/ {money(period.target_amount)}</span></p><div className="mt-2 h-2 overflow-hidden rounded bg-slate-200"><div className="h-full rounded bg-blue-600" style={{ width: `${percent}%` }} /></div><p className="mt-2 text-xs text-slate-500">还差 {salesProgress.currency} {money(period.remaining_amount)}</p></div>; })}</div><div className="mt-5 grid gap-5 border-t pt-5 xl:grid-cols-[1.1fr_0.9fr]"><div><h4 className="font-semibold">年度目标分析</h4><div className="mt-3 grid gap-3 sm:grid-cols-2"><p className="text-sm text-slate-600">CRM订单销售额 <strong className="ml-2 text-slate-900">{salesProgress?.currency} {money(salesProgress?.annual_analysis.crm_order_amount ?? "0")}</strong></p><p className="text-sm text-slate-600">手动补充销售额 <strong className="ml-2 text-slate-900">{salesProgress?.currency} {money(salesProgress?.annual_analysis.manual_amount ?? "0")}</strong></p><p className="text-sm text-slate-600">实际总销售额 <strong className="ml-2 text-slate-900">{salesProgress?.currency} {money(salesProgress?.annual_analysis.actual_total_amount ?? "0")}</strong></p><p className="text-sm text-slate-600">年度完成率 <strong className="ml-2 text-slate-900">{salesProgress?.annual_analysis.completion_percent === null ? "未设目标" : `${Number(salesProgress?.annual_analysis.completion_percent).toFixed(1)}%`}</strong></p><p className="text-sm text-slate-600">年度时间进度 <strong className="ml-2 text-slate-900">{Number(salesProgress?.annual_analysis.time_progress_percent ?? 0).toFixed(1)}%</strong></p><p className="text-sm text-slate-600">{salesProgress?.annual_analysis.pace_label ?? "计划进度"} <strong className="ml-2 text-slate-900">{salesProgress?.annual_analysis.pace_percent === null ? "未设目标" : `${Math.abs(Number(salesProgress?.annual_analysis.pace_percent)).toFixed(1)}%`}</strong></p><p className="text-sm text-slate-600">剩余年度目标 <strong className="ml-2 text-slate-900">{salesProgress?.currency} {money(salesProgress?.annual_analysis.remaining_amount ?? "0")}</strong></p><p className="text-sm text-slate-600">平均每月还需 <strong className="ml-2 text-slate-900">{salesProgress?.currency} {money(salesProgress?.annual_analysis.monthly_required_amount ?? "0")}</strong></p></div>{(salesProgress?.currency_breakdown.length ?? 0) > 1 && <p className="mt-4 text-xs text-slate-500">其他币种独立统计：{salesProgress?.currency_breakdown.filter((item) => item.currency !== salesProgress.currency).map((item) => `${item.currency} ${money(item.actual_amount)}`).join(" / ")}</p>}</div><div><h4 className="font-semibold">添加其他销售额</h4>{user?.role !== "Viewer" && <form onSubmit={(event) => void saveOtherSale(event)} className="mt-3 grid gap-2 sm:grid-cols-2"><input required type="date" value={otherDate} onChange={(event) => setOtherDate(event.target.value)} className="rounded border px-2 py-1.5 text-sm" /><input required type="number" min="0" step="0.01" value={otherAmount} onChange={(event) => setOtherAmount(event.target.value)} placeholder="金额" className="rounded border px-2 py-1.5 text-sm" /><input required value={otherCurrency} maxLength={3} onChange={(event) => setOtherCurrency(event.target.value.toUpperCase())} placeholder="币种" className="rounded border px-2 py-1.5 text-sm" /><input value={otherNote} onChange={(event) => setOtherNote(event.target.value)} placeholder="备注" className="rounded border px-2 py-1.5 text-sm" /><div className="sm:col-span-2 flex gap-2"><button disabled={busy} className="rounded bg-slate-900 px-3 py-1.5 text-sm font-semibold text-white disabled:opacity-60">{editingOtherId ? "保存修改" : "添加其他销售额"}</button>{editingOtherId && <button type="button" onClick={() => { setEditingOtherId(null); setOtherDate(""); setOtherAmount(""); setOtherNote(""); }} className="rounded border px-3 py-1.5 text-sm">取消</button>}</div></form>}<div className="mt-3 divide-y">{otherSales.slice(0, 5).map((item) => <div key={item.id} className="flex items-center justify-between gap-2 py-2 text-sm"><span className="truncate">{item.sale_date} · {item.currency} {money(item.amount)}{item.note ? ` · ${item.note}` : ""}</span>{user?.role !== "Viewer" && <span className="shrink-0 space-x-2"><button type="button" onClick={() => beginEditOtherSale(item)} className="text-blue-700">修改</button><button type="button" onClick={() => void removeOtherSale(item.id)} className="text-rose-600">删除</button></span>}</div>)}{!otherSales.length && <p className="py-3 text-sm text-slate-500">暂无手动补充销售额。</p>}</div></div></div></section>
  </>;
}
